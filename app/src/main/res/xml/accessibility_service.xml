<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuration for PhairPlayAccessibilityService.

    This was previously scoped as narrowly as possible, on the reasoning that the service only ever
    PUSHES focus and never needs to observe anything. That reasoning was wrong in one specific way,
    and it is why the HomeKit remote worked inside PhairPlay and nowhere else:

      canRetrieveWindowContent lets us read the ACTIVE window's nodes.
      flagRetrieveInteractiveWindows is what makes other applications' windows reachable at all.

    Without the second flag, findFocus() outside our own app returns null, dpad() has no node to
    move from, press() reports false, and the caller correctly falls back to PhairPlay's own window.
    So the remote appeared to do nothing on the launcher or in Netflix. The device log showed the
    service connected the whole time ("leaving foreground alone"), which is what made this look like
    a missing permission rather than a missing flag.

    typeAllMask is required alongside it: interactive-window retrieval is only populated for a
    service that is actually subscribed to the events keeping the window list current.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds|flagIncludeNotImportantViews"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />

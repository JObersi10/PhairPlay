<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuration for PhairPlayAccessibilityService.

    Scoped as narrowly as the framework allows. The service exists to PUSH focus around on request
    from the HomeKit remote, never to observe what is on screen, so it subscribes to no event types
    and declares no packages of interest.

    canRetrieveWindowContent is the one capability that cannot be avoided: moving focus requires
    reading the focused node in order to ask it what is next. accessibilityFlags is left at the
    default so no extra content is reported to us.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />

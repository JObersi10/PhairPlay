<?xml version="1.0" encoding="utf-8"?>
<!-- Circular backing behind a protocol glyph. Tinted per protocol in HomeFragment, so the three
     cards are told apart by colour before any text is read. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#FFFFFFFF" />
</shape>
